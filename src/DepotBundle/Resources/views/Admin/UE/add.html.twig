{% extends "admin.html.twig" %}

{% block main %}
    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <h1>Ajout d'une UE</h1>
            </div>
        </div>
        <div class="row">
            <div class="col-xs-12">
                <div class="item">
                    {{ form_start(form) }}
                        {{ form_widget(form) }}
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
{% endblock %}